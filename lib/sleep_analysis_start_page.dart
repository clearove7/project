import 'package:flutter/material.dart';

class SleepAnalysisStartPage extends StatelessWidget {
  const SleepAnalysisStartPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Sleep Analysis Start')));
}
