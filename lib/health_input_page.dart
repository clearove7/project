import 'package:flutter/material.dart';

class HealthInputPage extends StatelessWidget {
  const HealthInputPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Health Input')));
}
