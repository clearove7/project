import 'package:flutter/material.dart';

class MedicationInputPage extends StatelessWidget {
  const MedicationInputPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Medication Input')));
}
